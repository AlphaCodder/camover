//loading screen
$(window).on("load",function(){
    $(".loader").fadeOut("slow");
});

// //user auth
// firebase.auth().onAuthStateChanged(user => {
//     if (user) {
//         getUserData(user.uid)
//     }
// })

// console.log(uid);